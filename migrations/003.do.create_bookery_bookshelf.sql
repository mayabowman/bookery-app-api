CREATE TABLE bookery_bookshelf (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  user_id INTEGER REFERENCES bookery_users(id) ON DELETE CASCADE NOT NULL,
  book_id INTEGER REFERENCES bookery_books(id) ON DELETE CASCADE NOT NULL,
  review TEXT,
  rating INTEGER
)